#initial commands
git init
git add . 
git commit -m #""
git branch -M master main
git remote add origin #link
git push origin main
#routine
git checkout #branch
git pull #if behind
git status
git add / commit / push ...
